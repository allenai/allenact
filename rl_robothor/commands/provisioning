sudo apt-get install -y python3-venv && python3 -mvenv start && source test/bin/activate && pip install torch && python -c "import torch; print(torch.cuda.is_available())"
docker login
docker pull jordisai2/embodiedrl:latest
